# m0603-margo

Simple learning module **M0603** for laboratory work No. 6 on Node.js .
Exports the `send(message)' function`, which sends an email to a fixed address (`margouniver@mail.ru `) via the 'sendmail` package.

## Установка
```bash
npm install m0603-margo
